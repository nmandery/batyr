cmake_minimum_required(VERSION 2.8)

# use the directory of the cmakelists to have
# include paths containing the name of the component
set(RAPIDJSON_INCLUDE_DIR ${CMAKE_CURRENT_LIST_DIR} PARENT_SCOPE)


# vim: ft=cmake
